# Story 1.3: Sincronização Cron Diária (Lancamentos2026 → Supabase)

**Status:** Done
**Story ID:** 1.3
**Objective:** Implementar sincronização automática diária de transações via Vercel Cron Job

---

## 📋 Descrição

Criar Vercel Cron Job que executa diariamente lendo Lancamentos2026 via Google Sheets API. Valida dados (categoria conhecida, data válida, valor > 0), normaliza (lowercase, ISO format), insere/atualiza em `transactions` via upsert, registra em `sync_log` e implementa retry automático 3x em caso de erro.

**PRD Reference:** Seção "Google Sheets (Lancamentos2026) → Supabase" (p. 251-263)
**Architecture Reference:** Fluxo 3 "Sincronização Diária Automática"

---

## ✅ Acceptance Criteria

- [x] Vercel Cron Job configurado em vercel.json
- [x] Rota API `/api/sync/cron` implementada
- [x] Leitura de 5 colunas: Data, Tipo (Entrada/Saída), Categoria, Valor, Descrição
- [x] Validação: categoria conhecida, data válida (ISO), valor > 0
- [x] Normalização: categoria lowercase, data ISO format
- [x] Upsert em `transactions` (atualiza se existe por date+category+type, insere se novo)
- [x] Teste: 10 transações inseridas/atualizadas corretamente (testado em scripts/test-sync-cron.ts)
- [x] Log em `sync_log`: rows_processed, status, error_message
- [x] Retry automático 3x antes de marcar erro (exponential backoff: 1s, 2s, 4s)
- [x] Teste: Sync com erro → retry funciona e recupera (implementado com retryWithBackoff)
- [x] Teste: Dados duplicados via Sheets → upsert evita duplicatas (date+category+type é único)

---

## 🎯 Scope

**IN:**
- Configurar Vercel Cron Job (vercel.json)
- API route `/api/sync/cron`
- Validação de dados de Lancamentos2026
- Normalização (lowercase, ISO dates)
- Upsert em `transactions`
- Log em `sync_log`
- Retry automático 3x
- Error handling e notificação

**OUT:**
- UI com status de sync (Story 2.9 adiciona badge)
- Cron frequency > 1x/dia (Phase 2)
- Cron para outras sources (Phase 2)

---

## 📊 Estimativa

**Complexity:** High
**Points:** 8-10
**Estimated Hours:** 4-6h
**Type:** Data Sync / Cron / Backend

---

## 🔗 Dependências

**Bloqueador:** Story 1.1 (Supabase + Google Sheets API)
**Bloqueia:** Stories 2.2, 2.3, 2.4, 2.5, 2.6, 2.7, 2.9 (dados de transações)
**Related:** PRD (Lancamentos2026 structure), Architecture (Fluxo 3)

---

## 🐰 CodeRabbit Integration

**Mode:** light
**Severity Filter:** [CRITICAL, HIGH]
**Auto-Fix:** CRITICAL (max 2 iterations)
**Focus Areas:**
- SQL injection (parameterized queries)
- Retry logic robustness
- Data validation completeness
- Timezone handling (ISO dates)
- Concurrency (se múltiplos crons disparem)
- Logging adequado para troubleshooting

---

## ✔️ Criteria of Done

- [x] Todos os AC passaram
- [x] Cron job testado localmente com validação de dados (mock data)
- [x] Retry logic testado (exponential backoff implementado e testado)
- [x] Logs estruturados em sync_log (started_at, completed_at, status, error_message)
- [x] `npm run typecheck` passa ✅
- [x] `npm run lint` passa ✅
- [x] Documentação de troubleshooting em docs/SYNC-DOCUMENTATION.md
- [x] Story marked as "Ready for Review"

---

## 📁 File List

**Criados:**
- `app/api/sync/cron/route.ts` - GET endpoint para cron job (181 linhas)
- `lib/sync-transactions.ts` - Lógica de validação, normalização e processamento (267 linhas)
- `scripts/test-sync-cron.ts` - Script de teste manual (148 linhas)
- `tests/sync-cron.test.ts` - Testes unitários (327 linhas)
- `docs/SYNC-DOCUMENTATION.md` - Guia completo de troubleshooting (478 linhas)
- `vercel.json` - Configuração do Vercel Cron (8 linhas)

**Atualizados:**
- `lib/db.ts` - Adicionados métodos `upsert()` e `upsertBatch()` para transactions
- Nenhuma alteração em `google-sheets.ts` (readLancamentos() já existia e funciona)
- Nenhuma alteração em `types.ts` (tipos Transaction, SyncLog já estavam corretos)

---

## 📝 Dev Notes

- Vercel Cron syntax: `cron(0 8 * * *)` = 8h da manhã todo dia
- Sheet row_id deve ser rastreado para upsert (coluna implícita ou explícita)
- Retry com exponential backoff: 1s, 2s, 4s
- Timezone: Vercel usa UTC, considerar em logs
- Considerar rate limiting de Google Sheets API (quota diária)
- Log estruturado para análise (timestamp, status, counts, erros)

---

## 🤖 Dev Agent Record

**Agent:** Dex (@dev)
**Started:** 2026-02-20 13:56
**Implementation Tasks:**

- [x] Criar vercel.json com configuração de cron (0 8 * * * = 8h UTC)
- [x] Criar lib/sync-transactions.ts com validação, normalização e retry logic
- [x] Criar app/api/sync/cron/route.ts com handler GET para cron job
- [x] Atualizar lib/db.ts com métodos upsert/upsertBatch
- [x] Criar scripts/test-sync-cron.ts para testes manuais
- [x] Criar tests/sync-cron.test.ts com testes unitários
- [x] Criar docs/SYNC-DOCUMENTATION.md com guia de troubleshooting
- [x] Validar com `npm run typecheck` ✅
- [x] Validar com `npm run lint` ✅

**Design Decisions:**
- Upsert key: (date, category, type) ao invés de sheet_row_id (melhor rastreabilidade)
- Retry: 3x com exponential backoff (1s, 2s, 4s)
- Validation: Suporta datas em ISO e formato DD/MM/YYYY
- Amounts: Suporta `,` e `.` como separador decimal
- Status: Logs estruturados em sync_log (started_at, completed_at, duration)

**Notes:**
- Google Sheets API key é protegida em env var
- Supabase client usa anon key (read-only do frontend permitido)
- Retry logic implementado com backoff exponencial
- Validações são completas (categoria, data, valor > 0)

---

## 📋 Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-02-20 14:15 | Quinn (@qa) | QA Review PASS — Todos AC validados, segurança OK, retry logic robusto, docs completa. Marcado como Done. |
| 2026-02-20 13:56-14:15 | Dex (@dev) | Implementação completa: vercel.json, API route, validação, normalização, upsert, retry logic, testes e documentação |
| 2026-02-20 | River (@sm) | Story criada |

---

**Próximas stories:** 2.2 (KPIs), 2.9 (Badge Erro)

